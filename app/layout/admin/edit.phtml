
        <?php if ($this('error')): ?>
            <div class="alert alert-error">
                <?= htmlspecialchars($this('error')) ?>
            </div>
        <?php endif; ?>
        
        <?php if ($this('success')): ?>
            <div class="alert alert-success">
                <?= htmlspecialchars($this('success')) ?>
            </div>
        <?php endif; ?>
        
        <div class="form-container">
            <form method="POST" enctype="multipart/form-data">
                <div class="form-group">
                    <label for="title">Назва вірша: *</label>
                    <input type="text" 
                           id="title" 
                           name="title" 
                           value="<?= htmlspecialchars($this('poem')->get('title') ?? '') ?>" 
                           required>
                </div>
                
                <div class="form-group">
                    <label for="virsh">Текст вірша: *</label>
                    <textarea id="virsh" 
                              name="virsh" 
                              required><?= htmlspecialchars($this('poem')->get('virsh') ?? '') ?></textarea>
                </div>
                
                <div class="form-group">
                    <label for="youtube">YouTube посилання:</label>
                    <input type="text" 
                           id="youtube" 
                           name="youtube" 
                           value="<?= htmlspecialchars($this('poem')->get('youtube') ?? '') ?>"
                           placeholder="VIDEO ID">
                    <small style="color: #666; display: block; margin-top: 0.25rem;">
                        Формат: VIDEO_ID
                    </small>
                </div>
                
                <!--div class="form-group">
                    <label for="illustration">Ілюстрація:</label>
                    <?php if ($this('poem')->get('illustration')): ?>
                        <div style="margin-bottom: 1rem;">
                            <img src="/<?= htmlspecialchars($this('poem')->get('illustration')) ?>" 
                                 alt="Поточна ілюстрація" 
                                 style="max-width: 300px; height: auto; border-radius: 4px; border: 1px solid #ddd;">
                            <p style="color: #666; margin-top: 0.5rem;">Поточна ілюстрація (буде замінена при завантаженні нової)</p>
                        </div>
                    <?php endif; ?>
                    <input type="file" 
                           id="illustration" 
                           name="illustration" 
                           accept="image/*">
                    <small style="color: #666; display: block; margin-top: 0.25rem;">
                        Підтримуються формати: JPG, PNG, GIF, WEBP
                    </small>
                </div-->
                
                <div class="form-group">
                    <label>
                        <input type="checkbox" 
                               name="enabled" 
                               value="1" 
                               <?= ($this('poem')->get('enabled') ?? 1) ? 'checked' : '' ?>>
                        Опублікувати на сайті
                    </label>
                </div>
                
                <div class="form-actions">
                    <button type="submit" title="Зберегти">✓</button>
                    <a href="/admin/index" class="btn-cancel" title="Скасувати">←</a>
                </div>
            </form>
        </div>
